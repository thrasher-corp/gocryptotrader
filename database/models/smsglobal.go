// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Smsglobal is an object representing the database table.
type Smsglobal struct {
	SmsglobalID int64  `boil:"smsglobal_id" json:"smsglobal_id" toml:"smsglobal_id" yaml:"smsglobal_id"`
	ConfigID    int64  `boil:"config_id" json:"config_id" toml:"config_id" yaml:"config_id"`
	Enabled     bool   `boil:"enabled" json:"enabled" toml:"enabled" yaml:"enabled"`
	Username    string `boil:"username" json:"username" toml:"username" yaml:"username"`
	Password    string `boil:"password" json:"password" toml:"password" yaml:"password"`

	R *smsglobalR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L smsglobalL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SmsglobalColumns = struct {
	SmsglobalID string
	ConfigID    string
	Enabled     string
	Username    string
	Password    string
}{
	SmsglobalID: "smsglobal_id",
	ConfigID:    "config_id",
	Enabled:     "enabled",
	Username:    "username",
	Password:    "password",
}

// smsglobalR is where relationships are stored.
type smsglobalR struct {
}

// smsglobalL is where Load methods for each relationship are stored.
type smsglobalL struct{}

var (
	smsglobalColumns               = []string{"smsglobal_id", "config_id", "enabled", "username", "password"}
	smsglobalColumnsWithoutDefault = []string{"smsglobal_id", "config_id", "enabled", "username", "password"}
	smsglobalColumnsWithDefault    = []string{}
	smsglobalPrimaryKeyColumns     = []string{"smsglobal_id"}
)

type (
	// SmsglobalSlice is an alias for a slice of pointers to Smsglobal.
	// This should generally be used opposed to []Smsglobal.
	SmsglobalSlice []*Smsglobal

	smsglobalQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	smsglobalType                 = reflect.TypeOf(&Smsglobal{})
	smsglobalMapping              = queries.MakeStructMapping(smsglobalType)
	smsglobalPrimaryKeyMapping, _ = queries.BindMapping(smsglobalType, smsglobalMapping, smsglobalPrimaryKeyColumns)
	smsglobalInsertCacheMut       sync.RWMutex
	smsglobalInsertCache          = make(map[string]insertCache)
	smsglobalUpdateCacheMut       sync.RWMutex
	smsglobalUpdateCache          = make(map[string]updateCache)
	smsglobalUpsertCacheMut       sync.RWMutex
	smsglobalUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single smsglobal record from the query, and panics on error.
func (q smsglobalQuery) OneP() *Smsglobal {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single smsglobal record from the query.
func (q smsglobalQuery) One() (*Smsglobal, error) {
	o := &Smsglobal{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for smsglobal")
	}

	return o, nil
}

// AllP returns all Smsglobal records from the query, and panics on error.
func (q smsglobalQuery) AllP() SmsglobalSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all Smsglobal records from the query.
func (q smsglobalQuery) All() (SmsglobalSlice, error) {
	var o []*Smsglobal

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Smsglobal slice")
	}

	return o, nil
}

// CountP returns the count of all Smsglobal records in the query, and panics on error.
func (q smsglobalQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all Smsglobal records in the query.
func (q smsglobalQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count smsglobal rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q smsglobalQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q smsglobalQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if smsglobal exists")
	}

	return count > 0, nil
}

// SmsglobalsG retrieves all records.
func SmsglobalsG(mods ...qm.QueryMod) smsglobalQuery {
	return Smsglobals(boil.GetDB(), mods...)
}

// Smsglobals retrieves all the records using an executor.
func Smsglobals(exec boil.Executor, mods ...qm.QueryMod) smsglobalQuery {
	mods = append(mods, qm.From("\"smsglobal\""))
	return smsglobalQuery{NewQuery(exec, mods...)}
}

// FindSmsglobalG retrieves a single record by ID.
func FindSmsglobalG(smsglobalID int64, selectCols ...string) (*Smsglobal, error) {
	return FindSmsglobal(boil.GetDB(), smsglobalID, selectCols...)
}

// FindSmsglobalGP retrieves a single record by ID, and panics on error.
func FindSmsglobalGP(smsglobalID int64, selectCols ...string) *Smsglobal {
	retobj, err := FindSmsglobal(boil.GetDB(), smsglobalID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSmsglobal retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSmsglobal(exec boil.Executor, smsglobalID int64, selectCols ...string) (*Smsglobal, error) {
	smsglobalObj := &Smsglobal{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"smsglobal\" where \"smsglobal_id\"=$1", sel,
	)

	q := queries.Raw(exec, query, smsglobalID)

	err := q.Bind(smsglobalObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from smsglobal")
	}

	return smsglobalObj, nil
}

// FindSmsglobalP retrieves a single record by ID with an executor, and panics on error.
func FindSmsglobalP(exec boil.Executor, smsglobalID int64, selectCols ...string) *Smsglobal {
	retobj, err := FindSmsglobal(exec, smsglobalID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Smsglobal) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *Smsglobal) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *Smsglobal) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *Smsglobal) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no smsglobal provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(smsglobalColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	smsglobalInsertCacheMut.RLock()
	cache, cached := smsglobalInsertCache[key]
	smsglobalInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			smsglobalColumns,
			smsglobalColumnsWithDefault,
			smsglobalColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(smsglobalType, smsglobalMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(smsglobalType, smsglobalMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"smsglobal\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"smsglobal\" DEFAULT VALUES"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into smsglobal")
	}

	if !cached {
		smsglobalInsertCacheMut.Lock()
		smsglobalInsertCache[key] = cache
		smsglobalInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single Smsglobal record. See Update for
// whitelist behavior description.
func (o *Smsglobal) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single Smsglobal record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *Smsglobal) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the Smsglobal, and panics on error.
// See Update for whitelist behavior description.
func (o *Smsglobal) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the Smsglobal.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *Smsglobal) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	smsglobalUpdateCacheMut.RLock()
	cache, cached := smsglobalUpdateCache[key]
	smsglobalUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			smsglobalColumns,
			smsglobalPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update smsglobal, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"smsglobal\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, smsglobalPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(smsglobalType, smsglobalMapping, append(wl, smsglobalPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update smsglobal row")
	}

	if !cached {
		smsglobalUpdateCacheMut.Lock()
		smsglobalUpdateCache[key] = cache
		smsglobalUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q smsglobalQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q smsglobalQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for smsglobal")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SmsglobalSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SmsglobalSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SmsglobalSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SmsglobalSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), smsglobalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"smsglobal\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, smsglobalPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in smsglobal slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Smsglobal) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *Smsglobal) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *Smsglobal) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *Smsglobal) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no smsglobal provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(smsglobalColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()

	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	smsglobalUpsertCacheMut.RLock()
	cache, cached := smsglobalUpsertCache[key]
	smsglobalUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			smsglobalColumns,
			smsglobalColumnsWithDefault,
			smsglobalColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			smsglobalColumns,
			smsglobalPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert smsglobal, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(smsglobalPrimaryKeyColumns))
			copy(conflict, smsglobalPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"smsglobal\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(smsglobalType, smsglobalMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(smsglobalType, smsglobalMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert smsglobal")
	}

	if !cached {
		smsglobalUpsertCacheMut.Lock()
		smsglobalUpsertCache[key] = cache
		smsglobalUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single Smsglobal record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *Smsglobal) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single Smsglobal record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Smsglobal) DeleteG() error {
	if o == nil {
		return errors.New("models: no Smsglobal provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single Smsglobal record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *Smsglobal) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single Smsglobal record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Smsglobal) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no Smsglobal provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), smsglobalPrimaryKeyMapping)
	sql := "DELETE FROM \"smsglobal\" WHERE \"smsglobal_id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from smsglobal")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q smsglobalQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q smsglobalQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no smsglobalQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from smsglobal")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SmsglobalSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SmsglobalSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no Smsglobal slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SmsglobalSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SmsglobalSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no Smsglobal slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), smsglobalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"smsglobal\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, smsglobalPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from smsglobal slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *Smsglobal) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *Smsglobal) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Smsglobal) ReloadG() error {
	if o == nil {
		return errors.New("models: no Smsglobal provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Smsglobal) Reload(exec boil.Executor) error {
	ret, err := FindSmsglobal(exec, o.SmsglobalID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SmsglobalSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SmsglobalSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SmsglobalSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty SmsglobalSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SmsglobalSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	smsglobals := SmsglobalSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), smsglobalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"smsglobal\".* FROM \"smsglobal\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, smsglobalPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&smsglobals)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SmsglobalSlice")
	}

	*o = smsglobals

	return nil
}

// SmsglobalExists checks if the Smsglobal row exists.
func SmsglobalExists(exec boil.Executor, smsglobalID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"smsglobal\" where \"smsglobal_id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, smsglobalID)
	}

	row := exec.QueryRow(sql, smsglobalID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if smsglobal exists")
	}

	return exists, nil
}

// SmsglobalExistsG checks if the Smsglobal row exists.
func SmsglobalExistsG(smsglobalID int64) (bool, error) {
	return SmsglobalExists(boil.GetDB(), smsglobalID)
}

// SmsglobalExistsGP checks if the Smsglobal row exists. Panics on error.
func SmsglobalExistsGP(smsglobalID int64) bool {
	e, err := SmsglobalExists(boil.GetDB(), smsglobalID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SmsglobalExistsP checks if the Smsglobal row exists. Panics on error.
func SmsglobalExistsP(exec boil.Executor, smsglobalID int64) bool {
	e, err := SmsglobalExists(exec, smsglobalID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
