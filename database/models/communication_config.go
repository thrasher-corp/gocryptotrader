// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// CommunicationConfig is an object representing the database table.
type CommunicationConfig struct {
	ID                int64       `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name              string      `boil:"name" json:"name" toml:"name" yaml:"name"`
	Enabled           bool        `boil:"enabled" json:"enabled" toml:"enabled" yaml:"enabled"`
	Verbose           bool        `boil:"verbose" json:"verbose" toml:"verbose" yaml:"verbose"`
	AccountUsername   null.String `boil:"account_username" json:"account_username,omitempty" toml:"account_username" yaml:"account_username,omitempty"`
	AccountPassword   null.String `boil:"account_password" json:"account_password,omitempty" toml:"account_password" yaml:"account_password,omitempty"`
	TargetChannel     null.String `boil:"target_channel" json:"target_channel,omitempty" toml:"target_channel" yaml:"target_channel,omitempty"`
	VerificationToken null.String `boil:"verification_token" json:"verification_token,omitempty" toml:"verification_token" yaml:"verification_token,omitempty"`
	Host              null.String `boil:"host" json:"host,omitempty" toml:"host" yaml:"host,omitempty"`
	Port              null.String `boil:"port" json:"port,omitempty" toml:"port" yaml:"port,omitempty"`
	ConfigID          int64       `boil:"config_id" json:"config_id" toml:"config_id" yaml:"config_id"`

	R *communicationConfigR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L communicationConfigL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CommunicationConfigColumns = struct {
	ID                string
	Name              string
	Enabled           string
	Verbose           string
	AccountUsername   string
	AccountPassword   string
	TargetChannel     string
	VerificationToken string
	Host              string
	Port              string
	ConfigID          string
}{
	ID:                "id",
	Name:              "name",
	Enabled:           "enabled",
	Verbose:           "verbose",
	AccountUsername:   "account_username",
	AccountPassword:   "account_password",
	TargetChannel:     "target_channel",
	VerificationToken: "verification_token",
	Host:              "host",
	Port:              "port",
	ConfigID:          "config_id",
}

// CommunicationConfigRels is where relationship names are stored.
var CommunicationConfigRels = struct {
	Config string
}{
	Config: "Config",
}

// communicationConfigR is where relationships are stored.
type communicationConfigR struct {
	Config *Config
}

// NewStruct creates a new relationship struct
func (*communicationConfigR) NewStruct() *communicationConfigR {
	return &communicationConfigR{}
}

// communicationConfigL is where Load methods for each relationship are stored.
type communicationConfigL struct{}

var (
	communicationConfigColumns               = []string{"id", "name", "enabled", "verbose", "account_username", "account_password", "target_channel", "verification_token", "host", "port", "config_id"}
	communicationConfigColumnsWithoutDefault = []string{}
	communicationConfigColumnsWithDefault    = []string{"id", "name", "enabled", "verbose", "account_username", "account_password", "target_channel", "verification_token", "host", "port", "config_id"}
	communicationConfigPrimaryKeyColumns     = []string{"id"}
)

type (
	// CommunicationConfigSlice is an alias for a slice of pointers to CommunicationConfig.
	// This should generally be used opposed to []CommunicationConfig.
	CommunicationConfigSlice []*CommunicationConfig
	// CommunicationConfigHook is the signature for custom CommunicationConfig hook methods
	CommunicationConfigHook func(context.Context, boil.ContextExecutor, *CommunicationConfig) error

	communicationConfigQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	communicationConfigType                 = reflect.TypeOf(&CommunicationConfig{})
	communicationConfigMapping              = queries.MakeStructMapping(communicationConfigType)
	communicationConfigPrimaryKeyMapping, _ = queries.BindMapping(communicationConfigType, communicationConfigMapping, communicationConfigPrimaryKeyColumns)
	communicationConfigInsertCacheMut       sync.RWMutex
	communicationConfigInsertCache          = make(map[string]insertCache)
	communicationConfigUpdateCacheMut       sync.RWMutex
	communicationConfigUpdateCache          = make(map[string]updateCache)
	communicationConfigUpsertCacheMut       sync.RWMutex
	communicationConfigUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var communicationConfigBeforeInsertHooks []CommunicationConfigHook
var communicationConfigBeforeUpdateHooks []CommunicationConfigHook
var communicationConfigBeforeDeleteHooks []CommunicationConfigHook
var communicationConfigBeforeUpsertHooks []CommunicationConfigHook

var communicationConfigAfterInsertHooks []CommunicationConfigHook
var communicationConfigAfterSelectHooks []CommunicationConfigHook
var communicationConfigAfterUpdateHooks []CommunicationConfigHook
var communicationConfigAfterDeleteHooks []CommunicationConfigHook
var communicationConfigAfterUpsertHooks []CommunicationConfigHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CommunicationConfig) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CommunicationConfig) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CommunicationConfig) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CommunicationConfig) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CommunicationConfig) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CommunicationConfig) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CommunicationConfig) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CommunicationConfig) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CommunicationConfig) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range communicationConfigAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCommunicationConfigHook registers your hook function for all future operations.
func AddCommunicationConfigHook(hookPoint boil.HookPoint, communicationConfigHook CommunicationConfigHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		communicationConfigBeforeInsertHooks = append(communicationConfigBeforeInsertHooks, communicationConfigHook)
	case boil.BeforeUpdateHook:
		communicationConfigBeforeUpdateHooks = append(communicationConfigBeforeUpdateHooks, communicationConfigHook)
	case boil.BeforeDeleteHook:
		communicationConfigBeforeDeleteHooks = append(communicationConfigBeforeDeleteHooks, communicationConfigHook)
	case boil.BeforeUpsertHook:
		communicationConfigBeforeUpsertHooks = append(communicationConfigBeforeUpsertHooks, communicationConfigHook)
	case boil.AfterInsertHook:
		communicationConfigAfterInsertHooks = append(communicationConfigAfterInsertHooks, communicationConfigHook)
	case boil.AfterSelectHook:
		communicationConfigAfterSelectHooks = append(communicationConfigAfterSelectHooks, communicationConfigHook)
	case boil.AfterUpdateHook:
		communicationConfigAfterUpdateHooks = append(communicationConfigAfterUpdateHooks, communicationConfigHook)
	case boil.AfterDeleteHook:
		communicationConfigAfterDeleteHooks = append(communicationConfigAfterDeleteHooks, communicationConfigHook)
	case boil.AfterUpsertHook:
		communicationConfigAfterUpsertHooks = append(communicationConfigAfterUpsertHooks, communicationConfigHook)
	}
}

// One returns a single communicationConfig record from the query.
func (q communicationConfigQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CommunicationConfig, error) {
	o := &CommunicationConfig{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for communication_config")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CommunicationConfig records from the query.
func (q communicationConfigQuery) All(ctx context.Context, exec boil.ContextExecutor) (CommunicationConfigSlice, error) {
	var o []*CommunicationConfig

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CommunicationConfig slice")
	}

	if len(communicationConfigAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CommunicationConfig records in the query.
func (q communicationConfigQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count communication_config rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q communicationConfigQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if communication_config exists")
	}

	return count > 0, nil
}

// Config pointed to by the foreign key.
func (o *CommunicationConfig) Config(mods ...qm.QueryMod) configQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.ConfigID),
	}

	queryMods = append(queryMods, mods...)

	query := Configs(queryMods...)
	queries.SetFrom(query.Query, "\"config\"")

	return query
}

// LoadConfig allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (communicationConfigL) LoadConfig(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCommunicationConfig interface{}, mods queries.Applicator) error {
	var slice []*CommunicationConfig
	var object *CommunicationConfig

	if singular {
		object = maybeCommunicationConfig.(*CommunicationConfig)
	} else {
		slice = *maybeCommunicationConfig.(*[]*CommunicationConfig)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &communicationConfigR{}
		}
		args = append(args, object.ConfigID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &communicationConfigR{}
			}

			for _, a := range args {
				if a == obj.ConfigID {
					continue Outer
				}
			}

			args = append(args, obj.ConfigID)
		}
	}

	query := NewQuery(qm.From(`config`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Config")
	}

	var resultSlice []*Config
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Config")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for config")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for config")
	}

	if len(communicationConfigAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Config = foreign
		if foreign.R == nil {
			foreign.R = &configR{}
		}
		foreign.R.CommunicationConfigs = append(foreign.R.CommunicationConfigs, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ConfigID == foreign.ID {
				local.R.Config = foreign
				if foreign.R == nil {
					foreign.R = &configR{}
				}
				foreign.R.CommunicationConfigs = append(foreign.R.CommunicationConfigs, local)
				break
			}
		}
	}

	return nil
}

// SetConfig of the communicationConfig to the related item.
// Sets o.R.Config to related.
// Adds o to related.R.CommunicationConfigs.
func (o *CommunicationConfig) SetConfig(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Config) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"communication_config\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, []string{"config_id"}),
		strmangle.WhereClause("\"", "\"", 0, communicationConfigPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ConfigID = related.ID
	if o.R == nil {
		o.R = &communicationConfigR{
			Config: related,
		}
	} else {
		o.R.Config = related
	}

	if related.R == nil {
		related.R = &configR{
			CommunicationConfigs: CommunicationConfigSlice{o},
		}
	} else {
		related.R.CommunicationConfigs = append(related.R.CommunicationConfigs, o)
	}

	return nil
}

// CommunicationConfigs retrieves all the records using an executor.
func CommunicationConfigs(mods ...qm.QueryMod) communicationConfigQuery {
	mods = append(mods, qm.From("\"communication_config\""))
	return communicationConfigQuery{NewQuery(mods...)}
}

// FindCommunicationConfig retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCommunicationConfig(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*CommunicationConfig, error) {
	communicationConfigObj := &CommunicationConfig{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"communication_config\" where \"id\"=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, communicationConfigObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from communication_config")
	}

	return communicationConfigObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CommunicationConfig) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no communication_config provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(communicationConfigColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	communicationConfigInsertCacheMut.RLock()
	cache, cached := communicationConfigInsertCache[key]
	communicationConfigInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			communicationConfigColumns,
			communicationConfigColumnsWithDefault,
			communicationConfigColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(communicationConfigType, communicationConfigMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(communicationConfigType, communicationConfigMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"communication_config\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"communication_config\" () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT \"%s\" FROM \"communication_config\" WHERE %s", strings.Join(returnColumns, "\",\""), strmangle.WhereClause("\"", "\"", 0, communicationConfigPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into communication_config")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == communicationConfigMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for communication_config")
	}

CacheNoHooks:
	if !cached {
		communicationConfigInsertCacheMut.Lock()
		communicationConfigInsertCache[key] = cache
		communicationConfigInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CommunicationConfig.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CommunicationConfig) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	communicationConfigUpdateCacheMut.RLock()
	cache, cached := communicationConfigUpdateCache[key]
	communicationConfigUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			communicationConfigColumns,
			communicationConfigPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update communication_config, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"communication_config\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 0, wl),
			strmangle.WhereClause("\"", "\"", 0, communicationConfigPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(communicationConfigType, communicationConfigMapping, append(wl, communicationConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update communication_config row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for communication_config")
	}

	if !cached {
		communicationConfigUpdateCacheMut.Lock()
		communicationConfigUpdateCache[key] = cache
		communicationConfigUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q communicationConfigQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for communication_config")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for communication_config")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CommunicationConfigSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), communicationConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"communication_config\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, communicationConfigPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in communicationConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all communicationConfig")
	}
	return rowsAff, nil
}

// Delete deletes a single CommunicationConfig record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CommunicationConfig) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CommunicationConfig provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), communicationConfigPrimaryKeyMapping)
	sql := "DELETE FROM \"communication_config\" WHERE \"id\"=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from communication_config")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for communication_config")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q communicationConfigQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no communicationConfigQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from communication_config")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for communication_config")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CommunicationConfigSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CommunicationConfig slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(communicationConfigBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), communicationConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"communication_config\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, communicationConfigPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from communicationConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for communication_config")
	}

	if len(communicationConfigAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CommunicationConfig) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCommunicationConfig(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CommunicationConfigSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CommunicationConfigSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), communicationConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"communication_config\".* FROM \"communication_config\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, communicationConfigPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CommunicationConfigSlice")
	}

	*o = slice

	return nil
}

// CommunicationConfigExists checks if the CommunicationConfig row exists.
func CommunicationConfigExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"communication_config\" where \"id\"=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if communication_config exists")
	}

	return exists, nil
}
