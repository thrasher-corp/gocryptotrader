// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// TaxableEvent is an object representing the database table.
type TaxableEvent struct {
	TaxableEventsID                     int64     `boil:"taxable_events_id" json:"taxable_events_id" toml:"taxable_events_id" yaml:"taxable_events_id"`
	ConfigID                            int64     `boil:"config_id" json:"config_id" toml:"config_id" yaml:"config_id"`
	ConversionFrom                      string    `boil:"conversion_from" json:"conversion_from" toml:"conversion_from" yaml:"conversion_from"`
	ConversionFromAmount                float64   `boil:"conversion_from_amount" json:"conversion_from_amount" toml:"conversion_from_amount" yaml:"conversion_from_amount"`
	ConversionFromAmountEquivalantValue float64   `boil:"conversion_from_amount_equivalant_value" json:"conversion_from_amount_equivalant_value" toml:"conversion_from_amount_equivalant_value" yaml:"conversion_from_amount_equivalant_value"`
	ConversionTo                        string    `boil:"conversion_to" json:"conversion_to" toml:"conversion_to" yaml:"conversion_to"`
	ConversionToAmount                  float64   `boil:"conversion_to_amount" json:"conversion_to_amount" toml:"conversion_to_amount" yaml:"conversion_to_amount"`
	ConversionToamountEquivalantValue   float64   `boil:"conversion_toamount_equivalant_value" json:"conversion_toamount_equivalant_value" toml:"conversion_toamount_equivalant_value" yaml:"conversion_toamount_equivalant_value"`
	ConversionGainLoss                  float64   `boil:"conversion_gain_loss" json:"conversion_gain_loss" toml:"conversion_gain_loss" yaml:"conversion_gain_loss"`
	ExchangeName                        string    `boil:"exchange_name" json:"exchange_name" toml:"exchange_name" yaml:"exchange_name"`
	DateAndTime                         time.Time `boil:"date_and_time" json:"date_and_time" toml:"date_and_time" yaml:"date_and_time"`

	R *taxableEventR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L taxableEventL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TaxableEventColumns = struct {
	TaxableEventsID                     string
	ConfigID                            string
	ConversionFrom                      string
	ConversionFromAmount                string
	ConversionFromAmountEquivalantValue string
	ConversionTo                        string
	ConversionToAmount                  string
	ConversionToamountEquivalantValue   string
	ConversionGainLoss                  string
	ExchangeName                        string
	DateAndTime                         string
}{
	TaxableEventsID:                     "taxable_events_id",
	ConfigID:                            "config_id",
	ConversionFrom:                      "conversion_from",
	ConversionFromAmount:                "conversion_from_amount",
	ConversionFromAmountEquivalantValue: "conversion_from_amount_equivalant_value",
	ConversionTo:                        "conversion_to",
	ConversionToAmount:                  "conversion_to_amount",
	ConversionToamountEquivalantValue:   "conversion_toamount_equivalant_value",
	ConversionGainLoss:                  "conversion_gain_loss",
	ExchangeName:                        "exchange_name",
	DateAndTime:                         "date_and_time",
}

// taxableEventR is where relationships are stored.
type taxableEventR struct {
}

// taxableEventL is where Load methods for each relationship are stored.
type taxableEventL struct{}

var (
	taxableEventColumns               = []string{"taxable_events_id", "config_id", "conversion_from", "conversion_from_amount", "conversion_from_amount_equivalant_value", "conversion_to", "conversion_to_amount", "conversion_toamount_equivalant_value", "conversion_gain_loss", "exchange_name", "date_and_time"}
	taxableEventColumnsWithoutDefault = []string{"taxable_events_id", "config_id", "conversion_from", "conversion_from_amount", "conversion_from_amount_equivalant_value", "conversion_to", "conversion_to_amount", "conversion_toamount_equivalant_value", "conversion_gain_loss", "exchange_name", "date_and_time"}
	taxableEventColumnsWithDefault    = []string{}
	taxableEventPrimaryKeyColumns     = []string{"taxable_events_id"}
)

type (
	// TaxableEventSlice is an alias for a slice of pointers to TaxableEvent.
	// This should generally be used opposed to []TaxableEvent.
	TaxableEventSlice []*TaxableEvent

	taxableEventQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	taxableEventType                 = reflect.TypeOf(&TaxableEvent{})
	taxableEventMapping              = queries.MakeStructMapping(taxableEventType)
	taxableEventPrimaryKeyMapping, _ = queries.BindMapping(taxableEventType, taxableEventMapping, taxableEventPrimaryKeyColumns)
	taxableEventInsertCacheMut       sync.RWMutex
	taxableEventInsertCache          = make(map[string]insertCache)
	taxableEventUpdateCacheMut       sync.RWMutex
	taxableEventUpdateCache          = make(map[string]updateCache)
	taxableEventUpsertCacheMut       sync.RWMutex
	taxableEventUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single taxableEvent record from the query, and panics on error.
func (q taxableEventQuery) OneP() *TaxableEvent {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single taxableEvent record from the query.
func (q taxableEventQuery) One() (*TaxableEvent, error) {
	o := &TaxableEvent{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for taxable_events")
	}

	return o, nil
}

// AllP returns all TaxableEvent records from the query, and panics on error.
func (q taxableEventQuery) AllP() TaxableEventSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TaxableEvent records from the query.
func (q taxableEventQuery) All() (TaxableEventSlice, error) {
	var o []*TaxableEvent

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TaxableEvent slice")
	}

	return o, nil
}

// CountP returns the count of all TaxableEvent records in the query, and panics on error.
func (q taxableEventQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TaxableEvent records in the query.
func (q taxableEventQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count taxable_events rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q taxableEventQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q taxableEventQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if taxable_events exists")
	}

	return count > 0, nil
}

// TaxableEventsG retrieves all records.
func TaxableEventsG(mods ...qm.QueryMod) taxableEventQuery {
	return TaxableEvents(boil.GetDB(), mods...)
}

// TaxableEvents retrieves all the records using an executor.
func TaxableEvents(exec boil.Executor, mods ...qm.QueryMod) taxableEventQuery {
	mods = append(mods, qm.From("\"taxable_events\""))
	return taxableEventQuery{NewQuery(exec, mods...)}
}

// FindTaxableEventG retrieves a single record by ID.
func FindTaxableEventG(taxableEventsID int64, selectCols ...string) (*TaxableEvent, error) {
	return FindTaxableEvent(boil.GetDB(), taxableEventsID, selectCols...)
}

// FindTaxableEventGP retrieves a single record by ID, and panics on error.
func FindTaxableEventGP(taxableEventsID int64, selectCols ...string) *TaxableEvent {
	retobj, err := FindTaxableEvent(boil.GetDB(), taxableEventsID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTaxableEvent retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTaxableEvent(exec boil.Executor, taxableEventsID int64, selectCols ...string) (*TaxableEvent, error) {
	taxableEventObj := &TaxableEvent{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"taxable_events\" where \"taxable_events_id\"=$1", sel,
	)

	q := queries.Raw(exec, query, taxableEventsID)

	err := q.Bind(taxableEventObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from taxable_events")
	}

	return taxableEventObj, nil
}

// FindTaxableEventP retrieves a single record by ID with an executor, and panics on error.
func FindTaxableEventP(exec boil.Executor, taxableEventsID int64, selectCols ...string) *TaxableEvent {
	retobj, err := FindTaxableEvent(exec, taxableEventsID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TaxableEvent) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TaxableEvent) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TaxableEvent) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *TaxableEvent) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no taxable_events provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(taxableEventColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	taxableEventInsertCacheMut.RLock()
	cache, cached := taxableEventInsertCache[key]
	taxableEventInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			taxableEventColumns,
			taxableEventColumnsWithDefault,
			taxableEventColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(taxableEventType, taxableEventMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(taxableEventType, taxableEventMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"taxable_events\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"taxable_events\" DEFAULT VALUES"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into taxable_events")
	}

	if !cached {
		taxableEventInsertCacheMut.Lock()
		taxableEventInsertCache[key] = cache
		taxableEventInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single TaxableEvent record. See Update for
// whitelist behavior description.
func (o *TaxableEvent) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single TaxableEvent record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *TaxableEvent) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the TaxableEvent, and panics on error.
// See Update for whitelist behavior description.
func (o *TaxableEvent) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the TaxableEvent.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *TaxableEvent) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	taxableEventUpdateCacheMut.RLock()
	cache, cached := taxableEventUpdateCache[key]
	taxableEventUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			taxableEventColumns,
			taxableEventPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update taxable_events, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"taxable_events\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, taxableEventPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(taxableEventType, taxableEventMapping, append(wl, taxableEventPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update taxable_events row")
	}

	if !cached {
		taxableEventUpdateCacheMut.Lock()
		taxableEventUpdateCache[key] = cache
		taxableEventUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q taxableEventQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q taxableEventQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for taxable_events")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TaxableEventSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TaxableEventSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TaxableEventSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TaxableEventSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taxableEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"taxable_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, taxableEventPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in taxableEvent slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TaxableEvent) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TaxableEvent) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TaxableEvent) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *TaxableEvent) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no taxable_events provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(taxableEventColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()

	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	taxableEventUpsertCacheMut.RLock()
	cache, cached := taxableEventUpsertCache[key]
	taxableEventUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			taxableEventColumns,
			taxableEventColumnsWithDefault,
			taxableEventColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			taxableEventColumns,
			taxableEventPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert taxable_events, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(taxableEventPrimaryKeyColumns))
			copy(conflict, taxableEventPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"taxable_events\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(taxableEventType, taxableEventMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(taxableEventType, taxableEventMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert taxable_events")
	}

	if !cached {
		taxableEventUpsertCacheMut.Lock()
		taxableEventUpsertCache[key] = cache
		taxableEventUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single TaxableEvent record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TaxableEvent) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single TaxableEvent record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TaxableEvent) DeleteG() error {
	if o == nil {
		return errors.New("models: no TaxableEvent provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single TaxableEvent record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TaxableEvent) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single TaxableEvent record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TaxableEvent) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no TaxableEvent provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), taxableEventPrimaryKeyMapping)
	sql := "DELETE FROM \"taxable_events\" WHERE \"taxable_events_id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from taxable_events")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q taxableEventQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q taxableEventQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no taxableEventQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from taxable_events")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TaxableEventSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o TaxableEventSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no TaxableEvent slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TaxableEventSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TaxableEventSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no TaxableEvent slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taxableEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"taxable_events\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, taxableEventPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from taxableEvent slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TaxableEvent) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TaxableEvent) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TaxableEvent) ReloadG() error {
	if o == nil {
		return errors.New("models: no TaxableEvent provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TaxableEvent) Reload(exec boil.Executor) error {
	ret, err := FindTaxableEvent(exec, o.TaxableEventsID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TaxableEventSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TaxableEventSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TaxableEventSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty TaxableEventSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TaxableEventSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	taxableEvents := TaxableEventSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taxableEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"taxable_events\".* FROM \"taxable_events\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, taxableEventPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&taxableEvents)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TaxableEventSlice")
	}

	*o = taxableEvents

	return nil
}

// TaxableEventExists checks if the TaxableEvent row exists.
func TaxableEventExists(exec boil.Executor, taxableEventsID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"taxable_events\" where \"taxable_events_id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, taxableEventsID)
	}

	row := exec.QueryRow(sql, taxableEventsID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if taxable_events exists")
	}

	return exists, nil
}

// TaxableEventExistsG checks if the TaxableEvent row exists.
func TaxableEventExistsG(taxableEventsID int64) (bool, error) {
	return TaxableEventExists(boil.GetDB(), taxableEventsID)
}

// TaxableEventExistsGP checks if the TaxableEvent row exists. Panics on error.
func TaxableEventExistsGP(taxableEventsID int64) bool {
	e, err := TaxableEventExists(boil.GetDB(), taxableEventsID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TaxableEventExistsP checks if the TaxableEvent row exists. Panics on error.
func TaxableEventExistsP(exec boil.Executor, taxableEventsID int64) bool {
	e, err := TaxableEventExists(exec, taxableEventsID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
