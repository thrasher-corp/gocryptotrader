// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// GCTConfig is an object representing the database table.
type GCTConfig struct {
	ID         int64     `boil:"id" json:"id" toml:"id" yaml:"id"`
	ConfigName string    `boil:"config_name" json:"config_name" toml:"config_name" yaml:"config_name"`
	ConfigFull []byte    `boil:"config_full" json:"config_full" toml:"config_full" yaml:"config_full"`
	CreatedAt  time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	GCTUserID  int64     `boil:"gct_user_id" json:"gct_user_id" toml:"gct_user_id" yaml:"gct_user_id"`

	R *gctConfigR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L gctConfigL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GCTConfigColumns = struct {
	ID         string
	ConfigName string
	ConfigFull string
	CreatedAt  string
	UpdatedAt  string
	GCTUserID  string
}{
	ID:         "id",
	ConfigName: "config_name",
	ConfigFull: "config_full",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
	GCTUserID:  "gct_user_id",
}

// GCTConfigRels is where relationship names are stored.
var GCTConfigRels = struct {
	GCTUser string
}{
	GCTUser: "GCTUser",
}

// gctConfigR is where relationships are stored.
type gctConfigR struct {
	GCTUser *GCTUser
}

// NewStruct creates a new relationship struct
func (*gctConfigR) NewStruct() *gctConfigR {
	return &gctConfigR{}
}

// gctConfigL is where Load methods for each relationship are stored.
type gctConfigL struct{}

var (
	gctConfigColumns               = []string{"id", "config_name", "config_full", "created_at", "updated_at", "gct_user_id"}
	gctConfigColumnsWithoutDefault = []string{}
	gctConfigColumnsWithDefault    = []string{"id", "config_name", "config_full", "created_at", "updated_at", "gct_user_id"}
	gctConfigPrimaryKeyColumns     = []string{"id"}
)

type (
	// GCTConfigSlice is an alias for a slice of pointers to GCTConfig.
	// This should generally be used opposed to []GCTConfig.
	GCTConfigSlice []*GCTConfig
	// GCTConfigHook is the signature for custom GCTConfig hook methods
	GCTConfigHook func(context.Context, boil.ContextExecutor, *GCTConfig) error

	gctConfigQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	gctConfigType                 = reflect.TypeOf(&GCTConfig{})
	gctConfigMapping              = queries.MakeStructMapping(gctConfigType)
	gctConfigPrimaryKeyMapping, _ = queries.BindMapping(gctConfigType, gctConfigMapping, gctConfigPrimaryKeyColumns)
	gctConfigInsertCacheMut       sync.RWMutex
	gctConfigInsertCache          = make(map[string]insertCache)
	gctConfigUpdateCacheMut       sync.RWMutex
	gctConfigUpdateCache          = make(map[string]updateCache)
	gctConfigUpsertCacheMut       sync.RWMutex
	gctConfigUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var gctConfigBeforeInsertHooks []GCTConfigHook
var gctConfigBeforeUpdateHooks []GCTConfigHook
var gctConfigBeforeDeleteHooks []GCTConfigHook
var gctConfigBeforeUpsertHooks []GCTConfigHook

var gctConfigAfterInsertHooks []GCTConfigHook
var gctConfigAfterSelectHooks []GCTConfigHook
var gctConfigAfterUpdateHooks []GCTConfigHook
var gctConfigAfterDeleteHooks []GCTConfigHook
var gctConfigAfterUpsertHooks []GCTConfigHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *GCTConfig) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *GCTConfig) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *GCTConfig) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *GCTConfig) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *GCTConfig) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *GCTConfig) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *GCTConfig) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *GCTConfig) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *GCTConfig) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range gctConfigAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddGCTConfigHook registers your hook function for all future operations.
func AddGCTConfigHook(hookPoint boil.HookPoint, gctConfigHook GCTConfigHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		gctConfigBeforeInsertHooks = append(gctConfigBeforeInsertHooks, gctConfigHook)
	case boil.BeforeUpdateHook:
		gctConfigBeforeUpdateHooks = append(gctConfigBeforeUpdateHooks, gctConfigHook)
	case boil.BeforeDeleteHook:
		gctConfigBeforeDeleteHooks = append(gctConfigBeforeDeleteHooks, gctConfigHook)
	case boil.BeforeUpsertHook:
		gctConfigBeforeUpsertHooks = append(gctConfigBeforeUpsertHooks, gctConfigHook)
	case boil.AfterInsertHook:
		gctConfigAfterInsertHooks = append(gctConfigAfterInsertHooks, gctConfigHook)
	case boil.AfterSelectHook:
		gctConfigAfterSelectHooks = append(gctConfigAfterSelectHooks, gctConfigHook)
	case boil.AfterUpdateHook:
		gctConfigAfterUpdateHooks = append(gctConfigAfterUpdateHooks, gctConfigHook)
	case boil.AfterDeleteHook:
		gctConfigAfterDeleteHooks = append(gctConfigAfterDeleteHooks, gctConfigHook)
	case boil.AfterUpsertHook:
		gctConfigAfterUpsertHooks = append(gctConfigAfterUpsertHooks, gctConfigHook)
	}
}

// One returns a single gctConfig record from the query.
func (q gctConfigQuery) One(ctx context.Context, exec boil.ContextExecutor) (*GCTConfig, error) {
	o := &GCTConfig{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for gct_config")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all GCTConfig records from the query.
func (q gctConfigQuery) All(ctx context.Context, exec boil.ContextExecutor) (GCTConfigSlice, error) {
	var o []*GCTConfig

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to GCTConfig slice")
	}

	if len(gctConfigAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all GCTConfig records in the query.
func (q gctConfigQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count gct_config rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q gctConfigQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if gct_config exists")
	}

	return count > 0, nil
}

// GCTUser pointed to by the foreign key.
func (o *GCTConfig) GCTUser(mods ...qm.QueryMod) gctUserQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.GCTUserID),
	}

	queryMods = append(queryMods, mods...)

	query := GCTUsers(queryMods...)
	queries.SetFrom(query.Query, "\"gct_user\"")

	return query
}

// LoadGCTUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (gctConfigL) LoadGCTUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGCTConfig interface{}, mods queries.Applicator) error {
	var slice []*GCTConfig
	var object *GCTConfig

	if singular {
		object = maybeGCTConfig.(*GCTConfig)
	} else {
		slice = *maybeGCTConfig.(*[]*GCTConfig)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &gctConfigR{}
		}
		args = append(args, object.GCTUserID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &gctConfigR{}
			}

			for _, a := range args {
				if a == obj.GCTUserID {
					continue Outer
				}
			}

			args = append(args, obj.GCTUserID)
		}
	}

	query := NewQuery(qm.From(`gct_user`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load GCTUser")
	}

	var resultSlice []*GCTUser
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice GCTUser")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for gct_user")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for gct_user")
	}

	if len(gctConfigAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.GCTUser = foreign
		if foreign.R == nil {
			foreign.R = &gctUserR{}
		}
		foreign.R.GCTConfigs = append(foreign.R.GCTConfigs, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GCTUserID == foreign.ID {
				local.R.GCTUser = foreign
				if foreign.R == nil {
					foreign.R = &gctUserR{}
				}
				foreign.R.GCTConfigs = append(foreign.R.GCTConfigs, local)
				break
			}
		}
	}

	return nil
}

// SetGCTUser of the gctConfig to the related item.
// Sets o.R.GCTUser to related.
// Adds o to related.R.GCTConfigs.
func (o *GCTConfig) SetGCTUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *GCTUser) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"gct_config\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, []string{"gct_user_id"}),
		strmangle.WhereClause("\"", "\"", 0, gctConfigPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GCTUserID = related.ID
	if o.R == nil {
		o.R = &gctConfigR{
			GCTUser: related,
		}
	} else {
		o.R.GCTUser = related
	}

	if related.R == nil {
		related.R = &gctUserR{
			GCTConfigs: GCTConfigSlice{o},
		}
	} else {
		related.R.GCTConfigs = append(related.R.GCTConfigs, o)
	}

	return nil
}

// GCTConfigs retrieves all the records using an executor.
func GCTConfigs(mods ...qm.QueryMod) gctConfigQuery {
	mods = append(mods, qm.From("\"gct_config\""))
	return gctConfigQuery{NewQuery(mods...)}
}

// FindGCTConfig retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGCTConfig(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*GCTConfig, error) {
	gctConfigObj := &GCTConfig{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"gct_config\" where \"id\"=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, gctConfigObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from gct_config")
	}

	return gctConfigObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *GCTConfig) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no gct_config provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(gctConfigColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	gctConfigInsertCacheMut.RLock()
	cache, cached := gctConfigInsertCache[key]
	gctConfigInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			gctConfigColumns,
			gctConfigColumnsWithDefault,
			gctConfigColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(gctConfigType, gctConfigMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(gctConfigType, gctConfigMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"gct_config\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"gct_config\" () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT \"%s\" FROM \"gct_config\" WHERE %s", strings.Join(returnColumns, "\",\""), strmangle.WhereClause("\"", "\"", 0, gctConfigPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into gct_config")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == gctConfigMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for gct_config")
	}

CacheNoHooks:
	if !cached {
		gctConfigInsertCacheMut.Lock()
		gctConfigInsertCache[key] = cache
		gctConfigInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the GCTConfig.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *GCTConfig) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	gctConfigUpdateCacheMut.RLock()
	cache, cached := gctConfigUpdateCache[key]
	gctConfigUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			gctConfigColumns,
			gctConfigPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update gct_config, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"gct_config\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 0, wl),
			strmangle.WhereClause("\"", "\"", 0, gctConfigPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(gctConfigType, gctConfigMapping, append(wl, gctConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update gct_config row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for gct_config")
	}

	if !cached {
		gctConfigUpdateCacheMut.Lock()
		gctConfigUpdateCache[key] = cache
		gctConfigUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q gctConfigQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for gct_config")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for gct_config")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GCTConfigSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), gctConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"gct_config\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, gctConfigPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in gctConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all gctConfig")
	}
	return rowsAff, nil
}

// Delete deletes a single GCTConfig record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *GCTConfig) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no GCTConfig provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), gctConfigPrimaryKeyMapping)
	sql := "DELETE FROM \"gct_config\" WHERE \"id\"=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from gct_config")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for gct_config")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q gctConfigQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no gctConfigQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from gct_config")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for gct_config")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GCTConfigSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no GCTConfig slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(gctConfigBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), gctConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"gct_config\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, gctConfigPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from gctConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for gct_config")
	}

	if len(gctConfigAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *GCTConfig) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindGCTConfig(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GCTConfigSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GCTConfigSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), gctConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"gct_config\".* FROM \"gct_config\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, gctConfigPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in GCTConfigSlice")
	}

	*o = slice

	return nil
}

// GCTConfigExists checks if the GCTConfig row exists.
func GCTConfigExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"gct_config\" where \"id\"=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if gct_config exists")
	}

	return exists, nil
}
