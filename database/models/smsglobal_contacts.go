// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SmsglobalContact is an object representing the database table.
type SmsglobalContact struct {
	SmsglobalContactsID int64  `boil:"smsglobal_contacts_id" json:"smsglobal_contacts_id" toml:"smsglobal_contacts_id" yaml:"smsglobal_contacts_id"`
	ConfigID            int64  `boil:"config_id" json:"config_id" toml:"config_id" yaml:"config_id"`
	Name                string `boil:"name" json:"name" toml:"name" yaml:"name"`
	PhoneNumber         string `boil:"phone_number" json:"phone_number" toml:"phone_number" yaml:"phone_number"`
	Enabled             bool   `boil:"enabled" json:"enabled" toml:"enabled" yaml:"enabled"`

	R *smsglobalContactR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L smsglobalContactL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SmsglobalContactColumns = struct {
	SmsglobalContactsID string
	ConfigID            string
	Name                string
	PhoneNumber         string
	Enabled             string
}{
	SmsglobalContactsID: "smsglobal_contacts_id",
	ConfigID:            "config_id",
	Name:                "name",
	PhoneNumber:         "phone_number",
	Enabled:             "enabled",
}

// smsglobalContactR is where relationships are stored.
type smsglobalContactR struct {
}

// smsglobalContactL is where Load methods for each relationship are stored.
type smsglobalContactL struct{}

var (
	smsglobalContactColumns               = []string{"smsglobal_contacts_id", "config_id", "name", "phone_number", "enabled"}
	smsglobalContactColumnsWithoutDefault = []string{"smsglobal_contacts_id", "config_id", "name", "phone_number", "enabled"}
	smsglobalContactColumnsWithDefault    = []string{}
	smsglobalContactPrimaryKeyColumns     = []string{"smsglobal_contacts_id"}
)

type (
	// SmsglobalContactSlice is an alias for a slice of pointers to SmsglobalContact.
	// This should generally be used opposed to []SmsglobalContact.
	SmsglobalContactSlice []*SmsglobalContact

	smsglobalContactQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	smsglobalContactType                 = reflect.TypeOf(&SmsglobalContact{})
	smsglobalContactMapping              = queries.MakeStructMapping(smsglobalContactType)
	smsglobalContactPrimaryKeyMapping, _ = queries.BindMapping(smsglobalContactType, smsglobalContactMapping, smsglobalContactPrimaryKeyColumns)
	smsglobalContactInsertCacheMut       sync.RWMutex
	smsglobalContactInsertCache          = make(map[string]insertCache)
	smsglobalContactUpdateCacheMut       sync.RWMutex
	smsglobalContactUpdateCache          = make(map[string]updateCache)
	smsglobalContactUpsertCacheMut       sync.RWMutex
	smsglobalContactUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single smsglobalContact record from the query, and panics on error.
func (q smsglobalContactQuery) OneP() *SmsglobalContact {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single smsglobalContact record from the query.
func (q smsglobalContactQuery) One() (*SmsglobalContact, error) {
	o := &SmsglobalContact{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for smsglobal_contacts")
	}

	return o, nil
}

// AllP returns all SmsglobalContact records from the query, and panics on error.
func (q smsglobalContactQuery) AllP() SmsglobalContactSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SmsglobalContact records from the query.
func (q smsglobalContactQuery) All() (SmsglobalContactSlice, error) {
	var o []*SmsglobalContact

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SmsglobalContact slice")
	}

	return o, nil
}

// CountP returns the count of all SmsglobalContact records in the query, and panics on error.
func (q smsglobalContactQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SmsglobalContact records in the query.
func (q smsglobalContactQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count smsglobal_contacts rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q smsglobalContactQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q smsglobalContactQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if smsglobal_contacts exists")
	}

	return count > 0, nil
}

// SmsglobalContactsG retrieves all records.
func SmsglobalContactsG(mods ...qm.QueryMod) smsglobalContactQuery {
	return SmsglobalContacts(boil.GetDB(), mods...)
}

// SmsglobalContacts retrieves all the records using an executor.
func SmsglobalContacts(exec boil.Executor, mods ...qm.QueryMod) smsglobalContactQuery {
	mods = append(mods, qm.From("\"smsglobal_contacts\""))
	return smsglobalContactQuery{NewQuery(exec, mods...)}
}

// FindSmsglobalContactG retrieves a single record by ID.
func FindSmsglobalContactG(smsglobalContactsID int64, selectCols ...string) (*SmsglobalContact, error) {
	return FindSmsglobalContact(boil.GetDB(), smsglobalContactsID, selectCols...)
}

// FindSmsglobalContactGP retrieves a single record by ID, and panics on error.
func FindSmsglobalContactGP(smsglobalContactsID int64, selectCols ...string) *SmsglobalContact {
	retobj, err := FindSmsglobalContact(boil.GetDB(), smsglobalContactsID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSmsglobalContact retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSmsglobalContact(exec boil.Executor, smsglobalContactsID int64, selectCols ...string) (*SmsglobalContact, error) {
	smsglobalContactObj := &SmsglobalContact{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"smsglobal_contacts\" where \"smsglobal_contacts_id\"=$1", sel,
	)

	q := queries.Raw(exec, query, smsglobalContactsID)

	err := q.Bind(smsglobalContactObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from smsglobal_contacts")
	}

	return smsglobalContactObj, nil
}

// FindSmsglobalContactP retrieves a single record by ID with an executor, and panics on error.
func FindSmsglobalContactP(exec boil.Executor, smsglobalContactsID int64, selectCols ...string) *SmsglobalContact {
	retobj, err := FindSmsglobalContact(exec, smsglobalContactsID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SmsglobalContact) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SmsglobalContact) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SmsglobalContact) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *SmsglobalContact) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no smsglobal_contacts provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(smsglobalContactColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	smsglobalContactInsertCacheMut.RLock()
	cache, cached := smsglobalContactInsertCache[key]
	smsglobalContactInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			smsglobalContactColumns,
			smsglobalContactColumnsWithDefault,
			smsglobalContactColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(smsglobalContactType, smsglobalContactMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(smsglobalContactType, smsglobalContactMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"smsglobal_contacts\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"smsglobal_contacts\" DEFAULT VALUES"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into smsglobal_contacts")
	}

	if !cached {
		smsglobalContactInsertCacheMut.Lock()
		smsglobalContactInsertCache[key] = cache
		smsglobalContactInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single SmsglobalContact record. See Update for
// whitelist behavior description.
func (o *SmsglobalContact) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single SmsglobalContact record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *SmsglobalContact) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the SmsglobalContact, and panics on error.
// See Update for whitelist behavior description.
func (o *SmsglobalContact) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the SmsglobalContact.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *SmsglobalContact) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	smsglobalContactUpdateCacheMut.RLock()
	cache, cached := smsglobalContactUpdateCache[key]
	smsglobalContactUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			smsglobalContactColumns,
			smsglobalContactPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update smsglobal_contacts, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"smsglobal_contacts\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, smsglobalContactPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(smsglobalContactType, smsglobalContactMapping, append(wl, smsglobalContactPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update smsglobal_contacts row")
	}

	if !cached {
		smsglobalContactUpdateCacheMut.Lock()
		smsglobalContactUpdateCache[key] = cache
		smsglobalContactUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q smsglobalContactQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q smsglobalContactQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for smsglobal_contacts")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SmsglobalContactSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SmsglobalContactSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SmsglobalContactSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SmsglobalContactSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), smsglobalContactPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"smsglobal_contacts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, smsglobalContactPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in smsglobalContact slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SmsglobalContact) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SmsglobalContact) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SmsglobalContact) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *SmsglobalContact) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no smsglobal_contacts provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(smsglobalContactColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()

	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	smsglobalContactUpsertCacheMut.RLock()
	cache, cached := smsglobalContactUpsertCache[key]
	smsglobalContactUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			smsglobalContactColumns,
			smsglobalContactColumnsWithDefault,
			smsglobalContactColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			smsglobalContactColumns,
			smsglobalContactPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert smsglobal_contacts, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(smsglobalContactPrimaryKeyColumns))
			copy(conflict, smsglobalContactPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"smsglobal_contacts\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(smsglobalContactType, smsglobalContactMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(smsglobalContactType, smsglobalContactMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert smsglobal_contacts")
	}

	if !cached {
		smsglobalContactUpsertCacheMut.Lock()
		smsglobalContactUpsertCache[key] = cache
		smsglobalContactUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single SmsglobalContact record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SmsglobalContact) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single SmsglobalContact record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SmsglobalContact) DeleteG() error {
	if o == nil {
		return errors.New("models: no SmsglobalContact provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single SmsglobalContact record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SmsglobalContact) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single SmsglobalContact record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SmsglobalContact) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SmsglobalContact provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), smsglobalContactPrimaryKeyMapping)
	sql := "DELETE FROM \"smsglobal_contacts\" WHERE \"smsglobal_contacts_id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from smsglobal_contacts")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q smsglobalContactQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q smsglobalContactQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no smsglobalContactQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from smsglobal_contacts")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SmsglobalContactSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o SmsglobalContactSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no SmsglobalContact slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SmsglobalContactSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SmsglobalContactSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no SmsglobalContact slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), smsglobalContactPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"smsglobal_contacts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, smsglobalContactPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from smsglobalContact slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SmsglobalContact) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SmsglobalContact) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SmsglobalContact) ReloadG() error {
	if o == nil {
		return errors.New("models: no SmsglobalContact provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SmsglobalContact) Reload(exec boil.Executor) error {
	ret, err := FindSmsglobalContact(exec, o.SmsglobalContactsID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SmsglobalContactSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SmsglobalContactSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SmsglobalContactSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty SmsglobalContactSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SmsglobalContactSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	smsglobalContacts := SmsglobalContactSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), smsglobalContactPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"smsglobal_contacts\".* FROM \"smsglobal_contacts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, smsglobalContactPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&smsglobalContacts)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SmsglobalContactSlice")
	}

	*o = smsglobalContacts

	return nil
}

// SmsglobalContactExists checks if the SmsglobalContact row exists.
func SmsglobalContactExists(exec boil.Executor, smsglobalContactsID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"smsglobal_contacts\" where \"smsglobal_contacts_id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, smsglobalContactsID)
	}

	row := exec.QueryRow(sql, smsglobalContactsID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if smsglobal_contacts exists")
	}

	return exists, nil
}

// SmsglobalContactExistsG checks if the SmsglobalContact row exists.
func SmsglobalContactExistsG(smsglobalContactsID int64) (bool, error) {
	return SmsglobalContactExists(boil.GetDB(), smsglobalContactsID)
}

// SmsglobalContactExistsGP checks if the SmsglobalContact row exists. Panics on error.
func SmsglobalContactExistsGP(smsglobalContactsID int64) bool {
	e, err := SmsglobalContactExists(boil.GetDB(), smsglobalContactsID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SmsglobalContactExistsP checks if the SmsglobalContact row exists. Panics on error.
func SmsglobalContactExistsP(exec boil.Executor, smsglobalContactsID int64) bool {
	e, err := SmsglobalContactExists(exec, smsglobalContactsID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
