fmt := import("fmt")
exch := import("exchange")

name := "run"
timer := "5s"

load := func() {
    tx := exch.ticker("BTSE", "BTC-USD", "-", "SPOT")
    fmt.printf("TICKER DATA: %v\n", tx)
    cb := exch.ticker("CoinbasePro", "BTC-USD", "-", "SPOT")
    fmt.printf("TICKER DATA: %v\n", cb)
}

load()