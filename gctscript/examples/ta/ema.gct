fmt := import("fmt")
exch := import("exchange")
t := import("times")
sma := import("indicator/sma")
ema := import("indicator/ema")

load := func() {
    start := t.date(2019, 1 , 1 , 0 , 0 , 0, 0)
    end := t.add_date(start, 0, 6 , 0)
    ohlcvData := exch.ohlcv("coinbasepro", "BTC-USD", "-", "SPOT", start, end, "1d")

    ret := ema.calculate(ohlcvData.candles, 9)
    fmt.println(ret)

    ret = sma.calculate(ohlcvData.candles, 9)
    fmt.println(ret)
}

load()
