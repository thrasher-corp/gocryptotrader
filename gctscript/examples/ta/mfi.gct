fmt := import("fmt")
exch := import("exchange")
t := import("times")
mfi := import("indicator/mfi")

load := func() {
    start := t.add(t.now(), -t.hour*24*30)
    ohlcvData := exch.ohlcv("BTC Markets", "BTC-AUD", "-", "SPOT", start, t.now(), "1d")
    ret := mfi.calculate(ohlcvData.candles,14)
    fmt.println(ret)
}

load()
