fmt := import("fmt")
exch := import("exchange")
ind := import("indicator/rsi")

load := func() {
   ohlcvData := exch.ohlcv("BTC Markets", "BTC-AUD", "-", "a", "a", "a","a")
   ind.rsi(ohlcvData.candles, 10)
}

load()
